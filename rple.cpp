#include <bits/stdc++.h>
using namespace std;

#define all(x) (x).begin(), (x).end()
#define ll long long

void solve(){
	int n, r;
	cin >> n >> r;
	vector<pair<int, int>> a;
	for(int i=0; i<n; i++){
		int x, y;
		cin >> x >> y;
		a[i]= {x,y};
		
	}
	
	cin >> a >> b;
	
	for(int i=0; i<n; i++)
		cin >> a[i];
	
	for(int j=0; j<n; j++
		)
	
	
}
 
int main(){
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);	
	
	int tc=1;
	cin>>tc;
 
	while(tc--) solve();
	return 0;
}