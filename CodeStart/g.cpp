#include <bits/stdc++.h>
using namespace std;

#define all(x) (x).begin(), (x).end()
#define ll long long

void solve(){
	ll h, s;
	cin >> h >> s;
	
	ll a[h], b[s];
	
	for(ll i=0; i<h; i++)
		cin >> a[i];
	
	for(ll i=0; i<s; i++)
		cin >> b[i];
	
	
	
	
}
 
int main(){
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);	
	
	int tc=1;
	cin>>tc;
 
	while(tc--) solve();
	return 0;
}